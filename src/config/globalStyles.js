import { globalRingStyles } from './../plugins/ring'
import { globalKeyframeStyles } from './../plugins/animation'

const globalStyles = [globalKeyframeStyles, globalRingStyles]

export default globalStyles
